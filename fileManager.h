#ifndef FILEMANAGER_H
#define FILEMANAGER_H

#include <iostream>
#include <string>

class FileManager
{
    private:

    public:
    FileManager();
    ~FileManager();

    void saveToFile();
    void loadFromTheFile();

};

#endif // APPLICATIONDRIVER_H